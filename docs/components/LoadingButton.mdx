---
menu: Components
name: LoadingButton
route: /components/loadingButton
---

import {
  Playground,
  Props,
} from 'docz';
import { LoadingButton } from '@yeoubi/ui';

# LoadingButton

로딩 표시가 가능한 버튼 컴포넌트입니다.

<Props of={LoadingButton} />

## Usage

<Playground>
  <LoadingButton
    mr={16}
    ph={16}
    pv={8}
    level={2}
    color="#FFFFFF"
    bgColor="#212121"
    loadingProps={{
      border: '2px solid #FFFFFF',
      size: 16,
    }}
  >
    제출하기
  </LoadingButton>
  <LoadingButton
    ph={16}
    pv={8}
    level={2}
    color="#FFFFFF"
    bgColor="#212121"
    loadingProps={{
      border: '2px solid #FFFFFF',
      size: 16,
    }}
    isLoading
  >
    제출하기
  </LoadingButton>
</Playground>
