---
menu: Components
name: LoadingButton
route: /components/loadingButton
---

import {
  Playground,
  Props,
} from 'docz';
import {
  Space,
  LoadingButton,
} from '@yeoubi/ui';

# LoadingButton

로딩 표시가 가능한 버튼 컴포넌트입니다.

<Props of={LoadingButton} />

## Usage

<Playground>
  <Space mr={16} ph={16} pv={8}>
    <LoadingButton level={2} color="#FFFFFF" bgColor="#212121" loadingColor="#FFFFFF">
      제출하기
    </LoadingButton>
  </Space>
  <Space ph={16} pv={8}>
    <LoadingButton level={2} color="#FFFFFF" bgColor="#212121" loadingColor="#FFFFFF" isLoading>
      제출하기
    </LoadingButton>
  </Space>
</Playground>
